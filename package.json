{
  "name": "meu-quadro",
  "version": "1.0.0",
  "description": "Aplicativo de Lista de Tarefas",
  "main": "index.js",
  "scripts": {
    "start": "http-server -o -p 5500",
    "dev": "live-server --port=5501",
    "server": "json-server --watch data/tasks.json --port 3000",
    "test": "jest"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "jest": "^29.7.0",
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "meu-quadro": "file:"
  }
}
